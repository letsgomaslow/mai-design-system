# Historical blind builds superseded

The pre-1.0 HTML blind builds were removed because they encoded the retired pink-button and rounded-surface rules and included fictional example metrics.

Current blind-build outputs live in `artifacts/blind-build/`. They are generated by `scripts/build-artifacts.mjs`, contain explicit illustrative labels and sources, and pass release validation without unresolved variables.
